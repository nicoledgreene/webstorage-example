'use strict';

describe('myApp.sessionStorage module', function() {

  beforeEach(module('myApp.sessionStorage'));

  describe('sessionStorage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var sessionStorageCtrl = $controller('sessionStorageCtrl');
      expect(sessionStorageCtrl).toBeDefined();
    }));

  });
});