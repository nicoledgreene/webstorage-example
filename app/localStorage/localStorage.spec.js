'use strict';

describe('myApp.localStorage module', function() {

  beforeEach(module('myApp.localStorage'));

  describe('localStorage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var localStorageCtrl = $controller('localStorageCtrl');
      expect(localStorageCtrl).toBeDefined();
    }));

  });
});